{
  "name": "first-breath-engine",
  "version": "0.1.0",
  "description": "GTM research engine for Moment \u2014 collects real market signal through Bright Data (SERP API, Web Scraper API, Web Unlocker), keeps quotes verbatim and numbers computed, and feeds the scrollytelling landing page.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "collect": "node src/run.js --collect-only",
    "question": "node src/question.js",
    "play": "node src/play.js",
    "clusters": "node src/clusters.js",
    "verify": "node src/verify.js",
    "inject": "node src/run.js --inject-only",
    "ask": "node src/ask.js",
    "start": "node src/run.js"
  },
  "comment": ".env is auto-loaded by src/loadenv.js \u2014 no --env-file flag needed",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.60.0"
  }
}
